---
name: sync-helmfile
on:
  push:
    branches: [main]

jobs:
  skaffold:
    name: "skaffold"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - run: ls -la
